apiVersion: v1
kind: Service
metadata:
  name: mongo
spec:
  selector:
    app: taskmanager-mongo
  ports:
  - protocol: TCP
    port: 27017
    targetPort: 27017
  type: ClusterIP      # only accessible inside cluster